import { FocusBlockDirective } from './focus-block.directive';

describe('FocusBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusBlockDirective();
    expect(directive).toBeTruthy();
  });
});
