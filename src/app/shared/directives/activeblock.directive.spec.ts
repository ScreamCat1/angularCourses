import { ActiveblockDirective } from './activeblock.directive';

describe('ActiveblockDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveblockDirective();
    expect(directive).toBeTruthy();
  });
});
